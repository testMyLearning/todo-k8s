apiVersion: v1
kind: ConfigMap
metadata:
  name: pgbouncer-analytics-config
  namespace: todo-app
data:
  pgbouncer.ini: |
    [databases]
    analytics_db = host=postgres-analytics port=5432 dbname=analytics_db
    
    [pgbouncer]
    listen_port = 5432
    listen_addr = 0.0.0.0
    auth_type = trust
    auth_file = /etc/pgbouncer/userlist.txt
    pool_mode = transaction
    max_client_conn = 10000
    default_pool_size = 50
    server_idle_timeout = 600
    server_lifetime = 3600
    query_timeout = 30
    idle_transaction_timeout = 60

  userlist.txt: |
    "postgres" "postgres"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pgbouncer-analytics
  namespace: todo-app
  labels:
    app: pgbouncer-analytics
    component: pgbouncer
    service: analytics-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pgbouncer-analytics
  template:
    metadata:
      labels:
        app: pgbouncer-analytics
        component: pgbouncer
    spec:
      containers:
        - name: pgbouncer
          image: edoburu/pgbouncer:latest
          ports:
            - containerPort: 5432
          env:
            - name: DB_USER
              value: postgres
            - name: DB_PASSWORD
              value: postgres
            - name: DB_HOST
              value: postgres-analytics
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: analytics_db
            - name: POOL_MODE
              value: transaction
            - name: MAX_CLIENT_CONN
              value: "10000"
            - name: DEFAULT_POOL_SIZE
              value: "50"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 15
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/pgbouncer
      volumes:
        - name: config
          configMap:
            name: pgbouncer-analytics-config
---
apiVersion: v1
kind: Service
metadata:
  name: pgbouncer-analytics
  namespace: todo-app
  labels:
    app: pgbouncer-analytics
    component: pgbouncer
    service: analytics-db
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: pgbouncer
  selector:
    app: pgbouncer-analytics